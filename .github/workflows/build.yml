name: Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.4'
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: dot-time-apk
          path: build/app/outputs/flutter-apk/app-release.apk
```

## Step 3 — Push the workflow to GitHub
```
git add .
git commit -m "add github actions workflow"
git push
```

---

# How to Download APK After Every Push 📲

1. Go to your GitHub repo
2. Click **"Actions"** tab
3. Click the latest workflow run
4. Scroll down to **"Artifacts"**
5. Click **"dot-time-apk"** → downloads your APK!

---

## Your Complete Workflow Going Forward 🔥
```
Make changes in VS Code
       ↓
git add .
git commit -m "what you changed"
git push
       ↓
GitHub Actions builds APK automatically (5 mins)
       ↓
Download APK from Actions tab
       ↓
Install on phone!